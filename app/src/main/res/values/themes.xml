<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MVVMQuizJogador" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MVVMQuizJogador" parent="Base.Theme.MVVMQuizJogador" />

    <!-- Style do toolbar -->
    <style name="Widget.MyApp.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <!-- texto do titulo tambem via dimens -->
        <item name="titleTextAppearance">@style/TextAppearance.MyApp.Toolbar.Title</item>
    </style>

    <!-- TextAppearance para o titulo da toolbar -->
    <style name="TextAppearance.MyApp.Toolbar.Title"
        parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/toolbar_title_text_size</item>
    </style>
</resources>